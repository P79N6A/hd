<div class="form page-quick-sidebar-form">
    <!-- BEGIN FORM-->
    <form action="" method="post" class="form-horizontal form-bordered form-label-stripped form-editkeyword">
        <div class="page-quick-sidebar-title">
            <span class="page-quick-sidebar-titleinfo font-blue"><i class="fa fa-info"></i>修改热词信息</span>
            <div class="btn-group">
                <button type="submit" class="btn blue"><i class="fa fa-check"></i> 保存热词 </button>
            </div>
        </div>
        <div class="form-body">
            <?php $this->partial('partials/error_messages');?>

            <?php for ($i = 1 ; $i<=10 ; $i++) {?>
                <div class="form-group">
                    <label class="control-label col-md-2" style="padding-top: 15px;">热词<?= $i?> </label>
                    <div class="col-md-10">
                        <input type="text" name="keyword<?= $i?>" placeholder="热词<?= $i?>内容" value="<?= isset($data[$i-1]['name'])?$data[$i-1]['name']:''?>" maxlength="50"
                               class="form-control input-large maxlength-handler " style="display: inline"/>
                        &nbsp;&nbsp;权重&nbsp;&nbsp;
                        <input type="text" name="sort<?= $i?>" placeholder="热词<?= $i?>权重" value="<?= isset($data[$i-1]['sort'])?$data[$i-1]['sort']:''?>" maxlength="5"
                               class="form-control input-large maxlength-handler validate-box" style="display: inline"/>
                    </div>
                </div>
                <div class="form-group">
                    <label class="control-label col-md-2" style="padding-top: 15px;">关联的专辑id</label>
                    <div class="col-md-10">
                        <input type="text" name="relationship_id<?= $i?>" placeholder="关联的专辑id"
                               class="form-control relationship-subscription" value="<?= isset($data[$i-1]['set_id'])?$data[$i-1]['set_id']:''?>"/>
                    </div>
                </div>
            <?php }?>

        </div>
    </form>
</div>
<script type="text/javascript">
    $('.relationship-subscription').tagsInput({
        width: 'auto',
        defaultText: "输入标签"
    });
    // form stationsprogram
    $('.form-editkeyword').validate({
        errorElement: 'span', //default input error message container
        errorClass: 'help-block', // default input error message class
        focusInvalid: true, // do not focus the last invalid input
        rules: {
            sort1: {
                digits:true
            }
        },

        messages: {
            sort1: {
                digits: '权重必须输入整数'
            }
        },

        invalidHandler: function(event, validator) {  
            //$('.alert-danger', $('.login-form')).show();
        },

        highlight: function(element) { // hightlight error inputs
            $(element)
                .closest('.form-group').addClass('has-error'); // set error class to the control group
        },

        success: function(label) {
            label.closest('.form-group').removeClass('has-error');
            label.remove();
        },

        errorPlacement: function(error, element) {
            error.insertAfter(element.closest('.validate-box'));
        },

        submitHandler: function(form) {
            form.submit(); // form validation success, call ajax form submit
        }

    });
</script>
